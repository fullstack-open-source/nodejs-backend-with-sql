-- CreateTable
-- This is a baseline migration. The database schema is already in sync with the Prisma schema.
-- All tables (user, permission, group, group_permission, activity_log) already exist.
-- This migration serves as a baseline for future migrations.

-- No changes needed as database is already in sync
